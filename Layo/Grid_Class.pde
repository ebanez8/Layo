class Grid{
  float cellSize;
  boolean visible;
  
  Grid(float c, boolean v) {
    this.cellSize = c;
    this.visible = v;
  }
  
  void toggleVisibility() {}
  void grawGrid(float roomWidth, float roomHeight){}
  
}
